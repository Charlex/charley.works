$(document).ready(function(){
    $('header h1').circleType({
        radius: 1000,
        fitText:true,
        fitTextSize:1.2
    });
    $('header h2').fitText(2.5);
    $('#many-hats').circleType({
        radius: 1000,
        fitText:true,
        fitTextSize:1.4
    });
});
